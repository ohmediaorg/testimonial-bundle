{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Delete ' ~ testimonial %}

{% block breadcrumbs %}
{{ bootstrap_breadcrumbs(
  bootstrap_breadcrumb(bootstrap_icon('megaphone-fill') ~ ' Testimonials', 'testimonial_index'),
  bootstrap_breadcrumb('Delete'),
) }}
{% endblock %}
