{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Edit Testimonial' %}

{% block breadcrumbs %}
  {{ bootstrap_breadcrumbs(
    bootstrap_breadcrumb(bootstrap_icon('quote') ~ ' Testimonials', 'testimonial_index'),
    bootstrap_breadcrumb('Edit'),
  ) }}
{% endblock %}
